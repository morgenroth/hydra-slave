# Defaults for hydra-slave initscript
# sourced by /etc/init.d/hydra-slave
# installed at /etc/default/hydra-slave by the maintainer scripts

# Remove or set to '1' to enable starting hydra-slave daemon
ENABLED=0

# Configuration file for the daemon
DAEMON_CONF="/etc/hydra-slave.conf"

# Run the daemon as this specific user
DAEMON_USER="hydra"

# Define the name of the bridge between the slaves
BRIDGE_NAME="hydra0"
